releasever: 39

repos:
  - fedora
  - fedora-updates
  - bootc-fedora

metadata:
  name: fedora-ostree-base
  summary: Fedora base image

include:
  - base.yaml

repo-packages:
  - repo: bootc-fedora
    packages:
      - bootc

packages:
  - git
  - knot-resolver
  - knot-utils
