releasever: 39

repos:
  - fedora
  - fedora-updates

metadata:
  name: fedora-ostree-base
  summary: Fedora base image

include:
  - base.yaml

packages:
  # We include this for historical reasons
  - fedora-repos-ostree
