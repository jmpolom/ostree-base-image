releasever: 40

repos:
  - fedora-next
  - fedora-next-updates

metadata:
  name: fedora-ostree-base
  summary: Fedora base image

include:
  - base.yaml

packages:
  - git
  - knot-resolver
  - knot-utils
